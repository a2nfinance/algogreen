import { Button, Card } from "antd"

export const Credits = () => {
    return (
        <Card title="Carbon Offset Credits" style={{border: "none"}} headStyle={{padding: 0, textTransform: "uppercase"}} extra={
            <Button> More</Button>
        }>

        </Card>
    )
}